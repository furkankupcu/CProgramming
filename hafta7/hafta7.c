#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef unsigned long UL;

int main(void){

/*
char str1[5];
char str2[5];
int temp;
int boyut[5];

printf("sayilar:");
scanf("%s %s" ,str1,str2);

printf("%s %s\n",str1,str2);

fgets(str1,5,stdin);
printf("%s",str1);


strcpy(str1,"veli");
strcpy(str2,"ali");

temp = strcmp(str1,str2); //karşılaştırma

printf("%s\n %s\n",str1,str2);

printf("%d\n",temp);

printf("%ld %ld %ld\n",(UL) sizeof(str1),(UL) sizeof(str2), (UL) sizeof(temp));
*/












    return EXIT_SUCCESS;

}